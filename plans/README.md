# Implementation Plans

Generated by the improve skill on 2026-07-20 (against commit `cad1cc2`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

All three plans are independent (no ordering dependencies). They are numbered by
recommended execution order (highest leverage first), but any subset can be done
in any order.

## Execution order & status

| Plan | Title                                          | Priority | Effort | Depends on | Status |
|------|------------------------------------------------|----------|--------|------------|--------|
| 001  | Unit tests for RSS and canteen XML parsers     | P1       | M      | —          | DONE   |
| 002  | Add `.env.example` and document env variables  | P2       | S      | —          | DONE   |
| 003  | Deduplicate Android NFC permissions in app.json| P3       | S      | —          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None. All three plans touch disjoint files and can be executed independently
  and in parallel:
  - 001 → `lib/rssParser.ts`, `__tests__/lib/*`
  - 002 → `.env.example`, `README.md`
  - 003 → `app.json`

## Baseline (at planning time)

Verification baseline was clean when these plans were written:
- `npx tsc --noEmit` → exit 0
- `npm run lint` → exit 0
- `npx jest` → 17 suites, 78 tests, all pass

## Findings considered but NOT turned into plans

Presented to the maintainer; deliberately deferred or rejected:

- **No CI pipeline** (DX, HIGH leverage): `.github/` has only
  `copilot-instructions.md`, no workflow runs `lint`/`test`/`tsc`/`format` on
  PRs even though AGENTS.md mandates these gates. Was offered as a plan; the
  maintainer chose not to plan it this round. Strong candidate for a future
  pass — a GitHub Actions workflow is S effort, LOW risk.
- **RSS proxy route missing `Cache-Control`** (perf, minor):
  `app/rss/[key]+api.ts` re-fetches upstream on every request. The client
  already caches 15 min via TanStack Query, so impact is small. Not worth a
  dedicated plan now.
- **`npm audit` moderate advisories** (security): 14 moderate advisories, all in
  build-time dependencies (`@expo/config-plugins` and friends), none in runtime
  code shipped to the app. Per the audit playbook (report only critical/high on
  reachable runtime/build-distribution paths), this is low-signal noise. Not
  planned.
- **~28 `as any` casts** (tech-debt): almost all in the untyped-XML parsing code
  (`rssParser`, `canteenService`); lint-clean and localized. Not worth a
  consolidation plan.

## Direction findings (options for the maintainer, not defects)

Recorded for future strategy discussion; no plans written:

- **Decide the rides/carpool feature**: ~900+ lines fully wired
  (`RideMatchSheetContent.tsx` 710 LOC, `ridesService.ts`, `useRidesIndex.ts`,
  tests, scripts) but shipped off by default (`EXPO_PUBLIC_RIDES_ENABLED`
  defaults `false`). Either finish/ship it or remove the dead-flagged code.
- **Offline persistence across app restart**: `docs/offline-ux-plan.md` marks
  this out-of-scope and leaves two deferred items (global `onlineManager`,
  distinguishing "offline" vs "invalid course"). Would round out the offline UX.
</content>
